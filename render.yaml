services:
  - type: web
    name: side-quest-api
    runtime: node
    # NODE_ENV=production skips devDependencies; TypeScript must be present for tsc
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
